import { IconNameDirective } from './icon-name.directive';

describe('IconNameDirective', () => {
  it('should create an instance', () => {
    const directive = new IconNameDirective();
    expect(directive).toBeTruthy();
  });
});
